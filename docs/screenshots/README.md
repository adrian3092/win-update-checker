# Screenshots

Drop `gui.png` and `report.png` here and they'll render in the main README.

Suggested captures:
- **gui.png** — main window with several updates available, "Only show updates" enabled.
- **report.png** — the exported HTML report open in a browser.
